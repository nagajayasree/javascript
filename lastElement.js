//create function that takes array
//and returns last element.

let array = [1,2,5,8,3];
let output = lastElement(array);
console.log(output);

function lastElement(array){
    return array[array.length-1];
}
